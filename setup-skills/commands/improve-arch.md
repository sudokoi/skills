---
description: Scan and deepen codebase architecture
---
Load the improve-codebase-architecture skill and scan this codebase for deepening opportunities$([ -n "$ARGUMENTS" ] && printf ': %s' "$ARGUMENTS").

---
description: Decide or improve TypeScript codebase structure
---
Load the codebase-architecture skill and analyze or improve this codebase's structure$([ -n "$ARGUMENTS" ] && printf ': %s' "$ARGUMENTS").

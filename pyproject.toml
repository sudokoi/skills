[project]
name = "skills-validation"
version = "0.1.0"
description = "Pure-stdlib validation scripts for the skills repo"
requires-python = ">=3.8"

[tool.ruff]
line-length = 100
target-version = "py38"

[tool.ruff.lint]
select = ["E", "F", "W", "I", "UP"]